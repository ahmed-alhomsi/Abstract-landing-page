import styled from "styled-components";

export const StyledCard = styled.div`
    display: flex;
    justify-content: flex-start;
    margin: 1em;
    margin-bottom: 4em;
    width: 40%;

    p {
        max-width: 50ch;
    }
`